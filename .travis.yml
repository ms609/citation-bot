sudo: false
dist: trusty
branches:
  except: release

language: php
    
matrix:
  include:
    - stage: test
      php: 5.6 # Avoid downloading and installing 5.5
      script:
        - wget -O- https://tools.wmflabs.org/citations/gitpull.php ; fi
        - echo `wget -O- https://tools.wmflabs.org/citations-dev/gitpull.php` ; fi

git:
depth: 3
