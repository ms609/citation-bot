sudo: false
dist: trusty
branches:
  except: release

language: php
matrix:
  include:
      php: 7.3
      before_script:  
        - travis_retry composer self-update
        - composer require mediawiki/oauthclient:1.1.0
      script: phpunit

git:
  depth: 3
  
