sudo: false
dist: trusty
branches:
  except: release

language: php

matrix:
  include:
    - stage: test
      php: 7.3
      before_script:  
        - travis_retry composer self-update
        - composer require mediawiki/oauthclient
        - composer require --dev phpstan/phpstan
      script: vendor/bin/phpstan analyse --level 2  gadgetapi.php get_linked_pages.php generate_template.php process_page.php category.php

git:
  depth: 3
  
