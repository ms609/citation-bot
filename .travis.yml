sudo: false
dist: trusty
branches:
  except: release

language: php
    
matrix:
  include:
    - stage: test
      php: 5.6 # Avoid downloading and installing 5.5
      script:
        - wget -O- https://tools.wmflabs.org/citations/gitpull.php
        - echo `wget -O- https://tools.wmflabs.org/citations-dev/gitpull.php`

git:
depth: 3
