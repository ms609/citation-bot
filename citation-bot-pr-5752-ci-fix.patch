diff --git a/progpilot.json b/progpilot.json
index 8c2df43..637ad6b 100644
--- a/progpilot.json
+++ b/progpilot.json
@@ -6,10 +6,10 @@
             "$where"
         ],
         "source_line": [
-            18
+            24
         ],
         "source_column": [
-            855
+            1030
         ],
         "source_file": [
             "src\/authenticate.php"
@@ -17,38 +17,65 @@
         "tainted_flow": [
             [
                 {
-                    "flow_name": "$where",
-                    "flow_line": 88,
-                    "flow_column": 3570,
+                    "flow_name": "$return_path",
+                    "flow_line": 56,
+                    "flow_column": 1966,
                     "flow_file": "src\/authenticate.php"
                 },
                 {
-                    "flow_name": "$mb_trim_return",
-                    "flow_line": 88,
-                    "flow_column": 3579,
+                    "flow_name": "$_GET[\"return\"]",
+                    "flow_line": 56,
+                    "flow_column": 1981,
                     "flow_file": "src\/authenticate.php"
-                },
+                }
+            ]
+        ],
+        "sink_name": "header",
+        "sink_line": 31,
+        "sink_column": 1227,
+        "sink_file": "src\/authenticate.php",
+        "vuln_name": "header_injection",
+        "vuln_cwe": "CWE_601",
+        "vuln_id": "9928d98f544936ee4feaba8e361baf11727eb15e0501ba602da4b84607baf060",
+        "vuln_type": "taint-style"
+    }
+,
+    {
+        "source_name": [
+            "$where"
+        ],
+        "source_line": [
+            24
+        ],
+        "source_column": [
+            1030
+        ],
+        "source_file": [
+            "src\/authenticate.php"
+        ],
+        "tainted_flow": [
+            [
                 {
-                    "flow_name": "$mb_trim_param0_line88_column3579_progpilot",
-                    "flow_line": 88,
-                    "flow_column": 3579,
+                    "flow_name": "$return_path",
+                    "flow_line": 56,
+                    "flow_column": 1966,
                     "flow_file": "src\/authenticate.php"
                 },
                 {
                     "flow_name": "$_GET[\"return\"]",
-                    "flow_line": 88,
-                    "flow_column": 3587,
+                    "flow_line": 56,
+                    "flow_column": 1981,
                     "flow_file": "src\/authenticate.php"
                 }
             ]
         ],
         "sink_name": "header",
-        "sink_line": 22,
-        "sink_column": 1014,
+        "sink_line": 31,
+        "sink_column": 1227,
         "sink_file": "src\/authenticate.php",
         "vuln_name": "header_injection",
         "vuln_cwe": "CWE_601",
-        "vuln_id": "bc14c42f45fa81b9b165033a102ebc650c787db089224f7a481ac41d47af8cd0",
+        "vuln_id": "e729fcae158870cc6e5a3a74c9c96de6bea59b8b9c88a2d817f6b1c9cb3386d7",
         "vuln_type": "taint-style"
     }
 ,
diff --git a/psalm.xml b/psalm.xml
index cab2eea..973614f 100644
--- a/psalm.xml
+++ b/psalm.xml
@@ -5,7 +5,7 @@
     xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
     xmlns="https://getpsalm.org/schema/config"
     xsi:schemaLocation="https://getpsalm.org/schema/config vendor/vimeo/psalm/config.xsd"
-    autoloader="src/includes/setup.php"
+    autoloader="tests/staticAnalysisBootstrap.php"
     strictBinaryOperands="true"
     allowStringToStandInForClass="true"
     findUnusedVariablesAndParams="true"
diff --git a/src/includes/PublicConfig.php b/src/includes/PublicConfig.php
index 3123826..bea8762 100644
--- a/src/includes/PublicConfig.php
+++ b/src/includes/PublicConfig.php
@@ -58,7 +58,7 @@ function public_base_url(): string {
     }
 
     $parts = parse_url($configured);
-    if (!is_array($parts) || !isset($parts['scheme'], $parts['host']) || !is_string($parts['scheme']) || !is_string($parts['host'])) {
+    if (!is_array($parts) || !isset($parts['scheme'], $parts['host'])) {
         throw new RuntimeException('PUBLIC_BASE_URL must be an absolute URL');
     }
     $scheme = mb_strtolower($parts['scheme']);
@@ -72,7 +72,7 @@ function public_base_url(): string {
     }
 
     $path = $parts['path'] ?? '';
-    if (!is_string($path) || ($path !== '' && mb_substr($path, 0, 1) !== '/') || preg_match('~[\x00-\x20\x7f\\\\]~', $path)) {
+    if (($path !== '' && mb_substr($path, 0, 1) !== '/') || preg_match('~[\x00-\x20\x7f\\\\]~', $path)) {
         throw new RuntimeException('PUBLIC_BASE_URL contains an invalid path');
     }
     $path = mb_rtrim($path, '/');
@@ -145,7 +145,7 @@ function normalize_cors_origin(string $origin): ?string {
         return null;
     }
     $parts = parse_url($origin);
-    if (!is_array($parts) || !isset($parts['scheme'], $parts['host']) || !is_string($parts['scheme']) || !is_string($parts['host'])) {
+    if (!is_array($parts) || !isset($parts['scheme'], $parts['host'])) {
         return null;
     }
     $scheme = mb_strtolower($parts['scheme']);
diff --git a/tests/staticAnalysisBootstrap.php b/tests/staticAnalysisBootstrap.php
new file mode 100644
index 0000000..3b9bd6b
--- /dev/null
+++ b/tests/staticAnalysisBootstrap.php
@@ -0,0 +1,15 @@
+<?php
+
+declare(strict_types=1);
+
+if (getenv('PUBLIC_BASE_URL') === false) {
+    putenv('PUBLIC_BASE_URL=https://citations.toolforge.org');
+}
+if (getenv('ALLOWED_HOSTS') === false) {
+    putenv('ALLOWED_HOSTS=citations.toolforge.org');
+}
+if (getenv('ALLOWED_ORIGINS') === false) {
+    putenv('ALLOWED_ORIGINS=https://citations.toolforge.org,https://mdwiki.org,https://*.wikipedia.org');
+}
+
+require_once __DIR__ . '/../src/includes/setup.php';
diff --git a/tests/testBaseClass.php b/tests/testBaseClass.php
index 2ae3410..a373da4 100644
--- a/tests/testBaseClass.php
+++ b/tests/testBaseClass.php
@@ -4,13 +4,13 @@ declare(strict_types=1);
 ini_set('display_errors', 1);
 
 if (getenv('PUBLIC_BASE_URL') === false) {
-    putenv('PUBLIC_BASE_URL=https://citation-bot.example');
+    putenv('PUBLIC_BASE_URL=https://citations.toolforge.org');
 }
 if (getenv('ALLOWED_HOSTS') === false) {
-    putenv('ALLOWED_HOSTS=citation-bot.example');
+    putenv('ALLOWED_HOSTS=citations.toolforge.org');
 }
 if (getenv('ALLOWED_ORIGINS') === false) {
-    putenv('ALLOWED_ORIGINS=https://citation-bot.example,https://mdwiki.org,https://*.wikipedia.org');
+    putenv('ALLOWED_ORIGINS=https://citations.toolforge.org,https://mdwiki.org,https://*.wikipedia.org');
 }
 
 require_once __DIR__ . '/../src/includes/setup.php';
